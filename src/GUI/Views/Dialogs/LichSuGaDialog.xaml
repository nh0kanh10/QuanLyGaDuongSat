<Window x:Class="GUI.Views.Dialogs.LichSuGaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Lịch Sử Thay Đổi Bản Ghi Ga" 
        Height="480" Width="760"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#F8FAFC"
        FontFamily="Segoe UI">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header tiêu đề -->
            <RowDefinition Height="*"/>    <!-- DataGrid lịch sử -->
            <RowDefinition Height="Auto"/> <!-- Footer nút đóng -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="3" Padding="14,10" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:SymbolIcon Grid.Column="0" Symbol="History24" FontSize="24" Foreground="#003B73" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="txtTieuDe" Text="NHẬT KÝ THAY ĐỔI: GA HÀ NỘI (HNI)" FontSize="13" FontWeight="Bold" Foreground="#003B73"/>
                    <TextBlock Text="Toàn bộ lịch sử kiểm toán dữ liệu được tự động ghi nhận từ CSDL SQL Server (Múi giờ UTC+7)" FontSize="11" Foreground="#64748B" Margin="0,2,0,0"/>
                </StackPanel>
                <Border Grid.Column="2" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="2" Padding="8,4" VerticalAlignment="Center">
                    <TextBlock x:Name="txtSoBanGhi" Text="0 bản ghi" FontSize="11" FontWeight="Bold" Foreground="#334155"/>
                </Border>
            </Grid>
        </Border>

        <!-- DataGrid hiển thị Audit Trail -->
        <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="3">
            <DataGrid x:Name="dgNhatKy"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E2E8F0"
                      RowHeight="32">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Thời Gian Ghi (UTC+7)" Binding="{Binding ThoiGianDisplay}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontFamily" Value="Consolas"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="6,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Tài Khoản" Binding="{Binding TenDangNhap}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#003B73"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="6,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Hành Động" Binding="{Binding ThaoTac}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Foreground" Value="#D97706"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="6,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Chi Tiết / Nội Dung Ghi Nhận" Binding="{Binding GhiChu}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#334155"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                <Setter Property="Margin" Value="6,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Text="Dữ liệu lưu trữ bất biến phục vụ an toàn và thanh tra điều hành đường sắt." 
                       FontSize="11" FontStyle="Italic" Foreground="#94A3B8" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnDong" Content="Đóng" Width="90" Height="30" 
                        Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1" 
                        Foreground="#0F172A" FontWeight="SemiBold" Click="BtnDong_Click" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
