<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===================================================================== -->
    <!-- STYLE DÙNG CHUNG CHO CÁC DIALOG PHÂN HỆ KỸ THUẬT (Đạt)                 -->
    <!-- Đồng bộ màu với ThongSoHaTangDialog / QuanLyHaTangDialog của hệ thống -->
    <!-- ===================================================================== -->

    <!-- Khung ngoài cửa sổ không viền -->
    <Style x:Key="KtDialogKhung" TargetType="Border">
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="BorderBrush" Value="#003B73"/>
        <Setter Property="BorderThickness" Value="1.5"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="4" Opacity="0.3"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Dải tiêu đề kéo được -->
    <Style x:Key="KtHeader" TargetType="Border">
        <Setter Property="Background" Value="#003B73"/>
        <Setter Property="CornerRadius" Value="3,3,0,0"/>
    </Style>

    <!-- Nút X trên dải tiêu đề -->
    <Style x:Key="KtNutDong" TargetType="Button">
        <Setter Property="Width" Value="28"/>
        <Setter Property="Height" Value="26"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Foreground" Value="#FFFFFF"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="Transparent" CornerRadius="3">
                        <TextBlock Text="✕" FontSize="13" Foreground="{TemplateBinding Foreground}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#B91C1C"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Dải chân trang chứa nút thao tác -->
    <Style x:Key="KtFooter" TargetType="Border">
        <Setter Property="Background" Value="#F8FAFC"/>
        <Setter Property="BorderBrush" Value="#CBD5E1"/>
        <Setter Property="BorderThickness" Value="0,1,0,0"/>
        <Setter Property="CornerRadius" Value="0,0,3,3"/>
        <Setter Property="Padding" Value="16,10"/>
    </Style>

    <!-- Tiêu đề nhóm trường -->
    <Style x:Key="KtTieuDeMuc" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="#003B73"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <!-- Nhãn trường nhập -->
    <Style x:Key="KtNhanTruong" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="#334155"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
    </Style>

    <!-- Dòng lỗi dưới trường nhập (giữ sẵn chiều cao để form không bị giật) -->
    <Style x:Key="KtLoiTruong" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Foreground" Value="#B91C1C"/>
        <Setter Property="MinHeight" Value="15"/>
        <Setter Property="Margin" Value="0,2,0,6"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- Gợi ý nhỏ màu xám -->
    <Style x:Key="KtGoiY" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Foreground" Value="#94A3B8"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- Thẻ xem trước bên phải form -->
    <Style x:Key="KtTheXemTruoc" TargetType="Border">
        <Setter Property="Background" Value="#F8FAFC"/>
        <Setter Property="BorderBrush" Value="#E2E8F0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="3"/>
        <Setter Property="Padding" Value="11,9"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Style x:Key="KtNhanThongSo" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="#64748B"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="KtGiaTriThongSo" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="#0F172A"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Right"/>
    </Style>

    <!-- RadioButton dạng viên thuốc (chọn trạng thái / bộ lọc nhanh).
         Tag = mã màu chấm tròn bên trái. -->
    <Style x:Key="KtChipRadio" TargetType="RadioButton">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="#475569"/>
        <Setter Property="Margin" Value="0,0,6,6"/>
        <Setter Property="Tag" Value="#94A3B8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="Bd" Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1"
                            CornerRadius="12" Padding="11,4">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="Cham" Width="7" Height="7" Margin="0,0,6,0" VerticalAlignment="Center"
                                     Fill="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#64748B"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#003B73"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#003B73"/>
                            <Setter TargetName="Cham" Property="Stroke" Value="#FFFFFF"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#003B73"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
